using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;

/*
[CustomEditor(typeof( StopPoint))]
public class StopPointEditor : Editor
{
    StopPoint stopPoint;

    private void OnEnable()
    {
        stopPoint = (StopPoint)target;
    }

    public override void OnInspectorGUI()
    {
        base.OnInspectorGUI();

        if (GUILayout.Button("添加停止点"))
        {
            stopPoint.Add();
        }

        if (GUILayout.Button("试试停止点啥效果"))
        {
            stopPoint.Tryyyyyyyyyyyyyyyyy();
        }
        if (GUILayout.Button("移除某个停止点"))
        {
            stopPoint.RemoveEditorPoint(false);
        }
        if (GUILayout.Button("移除所有停止点"))
        {
            stopPoint.RemoveEditorPoint(true);
        }
    }
}
*/